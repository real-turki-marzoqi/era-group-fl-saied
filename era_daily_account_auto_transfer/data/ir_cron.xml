<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="ir_cron_auto_transfer_day" model="ir.cron">
            <field name="name">Automatic Daily Account Transfers</field>
            <field name="model_id" ref="account_auto_transfer.model_account_transfer_model"/>
            <field name="state">code</field>
            <field name="code">model.action_cron_auto_transfer()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="nextcall" eval="(DateTime.now() + relativedelta(days=1)).replace(hour=5, minute=0, second=0, microsecond=0).strftime('%Y-%m-%d %H:%M:%S')"/>
        </record>

    </data>
</odoo>
