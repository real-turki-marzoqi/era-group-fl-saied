<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="era_invoice_type_view_move_form" model="ir.ui.view">
            <field name="name">era.invoice.type.account.move.form</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">

                <xpath expr="//field[@name='invoice_vendor_bill_id']" position="after">
                    <field name="invoice_type"
                           invisible="move_type not in ['in_invoice', 'in_refund']"
                           readonly="state in ['cancel', 'posted']"/>
                </xpath>

            </field>
        </record>

        <record id="era_invoice_type_view_move_search" model="ir.ui.view">
            <field name="name">era.invoice.type.account.move.search</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_account_invoice_filter"/>
            <field name="arch" type="xml">
                <field name="invoice_user_id" position="after">
                    <field name="invoice_type"/>
                </field>
                <xpath expr="//group/filter[@name='status']" position="after">
                    <filter string="Invoice type" name="invoice_type" domain="[]" context="{'group_by': 'invoice_type'}"/>
                </xpath>
            </field>
        </record>


    </data>
</odoo>
